@explore_option{
    strategy = BFS;
    loggers = [graphic=svg];
    pre_filters = [ max_loop_depth = 3, 
                    max_node_number = 12 ]
}
@analyze_option{
    strategy = BFS;
    loggers = [graphic=svg]
}
@message{
    m
}
@lifeline{
    a;
    b
}
loop_seq(
    a -- m -> b
)